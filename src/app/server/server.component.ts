import { Component } from '@angular/core';

@Component({
  //selector where we can use the component in other component files
  selector: 'app-server',
  //contain the html code of the component
  templateUrl: './server.component.html',
})
export class ServerComponent {}
